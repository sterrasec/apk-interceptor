<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET permission is intentionally omitted. -->

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApkInterceptor">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".InterceptActivity"
            android:exported="true"
            android:theme="@style/Theme.ApkInterceptor.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${interceptScheme}" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.PayloadProvider"
            android:authorities="${applicationId}.payload"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
